<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <!-- Background -->
  <rect width="512" height="512" fill="#6366f1" rx="80"/>

  <!-- Target Icon -->
  <g transform="translate(256, 256)">
    <!-- Outer circle -->
    <circle r="140" fill="none" stroke="#ffffff" stroke-width="16"/>

    <!-- Middle circle -->
    <circle r="95" fill="none" stroke="#ffffff" stroke-width="16"/>

    <!-- Inner circle -->
    <circle r="50" fill="none" stroke="#ffffff" stroke-width="16"/>

    <!-- Center dot -->
    <circle r="20" fill="#ffffff"/>
  </g>

  <!-- Text -->
  <text x="256" y="420" font-family="Arial, sans-serif" font-size="48" font-weight="bold" fill="#ffffff" text-anchor="middle">HabitAlgo</text>
</svg>
